<view class="page">
  <view class="title">滑雪相册</view>
  <view class="subtitle">记录雪场打卡、训练瞬间和旅拍照片。</view>
  <view class="section photo-grid">
    <image wx:for="{{photos}}" wx:key="*this" class="photo" src="{{item}}" mode="aspectFill" />
  </view>
</view>
